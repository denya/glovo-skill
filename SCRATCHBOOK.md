# Scratchbook

## 2026-07-17

- v0.2.0 evaluation decision from the private local corpus: 924 captured cards yielded 910 usable completed venue events after 14 cancellations. Chronological 637/136/137 train/validation/final-test selection chose multi-scale recency with 5/20/80 order-event half-lives. Final Recall@5 was 41.6% versus 20.4% for popularity; NDCG@5 was 0.271 versus 0.160. No private orders, venue names, item names, ids, or locations will be copied into this repo.
- Product-model negative: only 15 successfully detailed orders exist, split 10/2/3. Validation had no repeatable item signal, so a learned item model is underpowered and must not be promoted. Product suggestions will resolve from current Glovo catalog evidence after venue selection.
- Ametller methodology reused deliberately: chronological rolling origins, repeat/explore separation, simple baselines, untouched final holdout, and simplest-within-one-standard-error selection. Ametller's receipt cleaning, exact grocery SKU model, calendar-day cadence, and learned product weights do not transfer to Glovo's card-heavy order history.
- `GOOGLE_MAPS_API_KEY` is currently unset on this host. Google contract tests must use sanitized fixtures; live Google evidence remains skipped unless a real configured key is present.
- Public venue-only harness reproduced the private aggregate exactly without copying data: 910 usable events, 637/136/137 split, validation-selected `multiscale_5_20_80`, final Recall@5 `0.4161`, NDCG@5 `0.2712`, and popularity Recall@5 `0.2044` / NDCG@5 `0.1599`.
- Fresh authenticated card-only evaluation reached cursor exhaustion at 946 unique cards over 64 pages, with 931 usable venue events after exclusions. The same model was selected on the new 651/140/140 split and reached final Recall@5 `0.4500` / NDCG@5 `0.3003`, versus popularity `0.2214` / `0.1796`. This is drift evidence, not a replacement for the frozen release benchmark.
- Live global search contract: `POST /v1/web/store_wall/search?searchQuery=...` with a generated search context returns store cards and product carousels. Compact parsing preserves store/address ids plus all three current product ids, category/search metadata, live ranking, Glovo rating/count, and open state; identifiers were never written into fixtures or logs.
- Google Places contract uses Text Search (New) only after Glovo shortlist generation, with an exact minimal field mask. Explicit review requests use Place Details for at most three finalists and preserve author/source notices. Google calls are mocked because no key is configured; no live Google pass is claimed.
- Negative live MCP finding: the first suggestion smoke encountered one bounded order-list 429 and exceeded the SDK's default 60-second request timeout while the server was correctly backing off. The sanitized harness now gives long-running full-history calls a bounded 240-second protocol timeout; the API retry remains bounded and does not retry basket mutations.
- Final authenticated MCP suggestion smoke passed through the rebuilt API-only bundle: 3 choices, 946 unique cards, 64 cursor pages, `no_next_cursor`, all final products freshly enabled, option groups preserved, Google unavailable because no key is configured, and `mutates_basket=false`. A private canonical basket comparison before/after reported only `basket_unchanged=true`; no basket write or checkout call occurred.
- Final local `npm run verify` passed at v0.2.0: deterministic 8.0 MB bundle, deterministic/unit contract suite, strict Claude plugin and MCPB validation, a 7-file/1.4 MB MCPB without source or `node_modules`, guest direct-API MCP smoke, isolated packaged no-browser startup, zero high-severity dependency vulnerabilities, and a 43-file secret scan including Google-key detection.
- Independent gitleaks initially flagged Playwright's bundled RFC 6455 sample WebSocket nonce as a generic key in both the old public commit and current bundle. Added a rule-scoped exact-value allowlist for that public standard sample only; generic key detection remains enabled elsewhere. Final scans covered all 17 commits plus the working directory and found no leaks.
- Published `v0.2.0` from commit `b98d93c2515d4f9956d2f6e03d3fb03f0d84e2e8` via GitHub CLI. The seven-file Desktop asset is `glovo-skill-0.2.0.mcpb`, SHA256 `a519c63bb13b059f85c246d54aac01a9685e00b616308d8484090c83b84ee14f`. Anonymous download matched the frozen local bytes and the unpacked public asset passed isolated no-browser startup. The same bytes were copied to the user's local iCloud `Inbox`; no MCPB is committed to git.
- Final public proof: a clean HTTPS clone at main commit `c915490944c3c2398949bc7982f6a7f753bfa134` passed `npm ci` and the complete `npm run verify` gate. A fresh `CLAUDE_CONFIG_DIR` installed `glovo@denya-glovo` v0.2.0 from the public marketplace and its cached bundle passed no-browser startup with the optional Google key unset. A fresh temporary `CODEX_HOME` also registered the public clone as a stdio MCP successfully.
- Negative harness invocation finding: `npm ci --prefix <clone>` from inside another package produced a misleading lock mismatch under the installed npm version. Running the documented `cd glovo-skill && npm ci` flow in the clean clone passed; the package and lock names/versions were already aligned and no corrective release was needed.

- Created clean repo at `/Users/denya/code/glovo-skill` from installed extension source.
- Excluded `node_modules`, `dist`, bundles, generated order data, and `~/.glovo/session.json`.
- Prior validated order contract from knowledge-base work: `/v3/customer/orders-list` starts at `offset=0`, page cap observed as 15, next page uses `pagination.next.offset`; one run found 924 unique orders over 62 pages.
- Known wrong assumption: numeric order offset increments are invalid for order history.
- Known rate-limit effect: full order-detail enrichment hit HTTP 429 after a small number of detail calls; discovery must persist before detail enrichment.
- GitHub CLI auth check failed: local `gh` token for `denya` is invalid. Publishing needs refresh or another authenticated path.
- Cross-feed from Ametller: both `~/.ametller/session.json` and `~/.glovo/session.json` are currently mode `0600`, but regression coverage must keep load-time chmod migration so future drift is corrected.
- Claude marketplace installs do not run `npm install`; plugin and MCPB must point at the same committed self-contained stdio bundle, with source plus deterministic rebuild verification.
- Publication is not proven until immediately-before-publish checks pass. Need distinguish invalid `gh` API auth from transient Git network reachability; a prior Ametller dry-run also saw `github.com:443` unreachable.
- QA checkpoint: legacy `manifest.json` still uses `dxt_version: 0.1` and author `Local Codex`; MCPB is not complete until converted to `manifest_version: 0.3` and validated/packed.
- QA checkpoint: `tests/order-contract.test.mjs` initially covered only stats; it must prove cursor walking uses exact `next_offset` and bounded 429 retry without real sleeps.
- QA checkpoint: MCP surface is not complete until location/full-history/stats tools are registered and exercised through MCP, not just API helpers.
- QA checkpoint: order stats must separate card-only discovery from detail coverage and only report visible totals when parseable.
- QA checkpoint: modifier E2E must validate live product-view options immediately before add, assert basket representation, and restore in `finally`.
- Local MCP smoke started the bundled server and listed new tools, including location and order stats. Live `glovo_browse_stores` failed because `fetch('https://api.glovoapp.com')` returns `ECONNREFUSED`; live gates remain open until network works.
- Updated build to the shared selective Playwright bundle: only `chromium-bidi/lib/cjs/bidiMapper/BidiMapper` and `chromium-bidi/lib/cjs/cdp/CdpConnection` remain external. Bundle is 7.9 MB and starts from `/tmp` with `NODE_PATH` unset and no adjacent `node_modules`.
- MCP smoke against the self-contained bundle passed and listed location/order-stats tools. Live browse recovered and returned stores; first grocery smoke store was closed/empty, so full E2E still needs search fallback.
- WebDriver-BiDi is not a supported login mode unless Chrome login/E2E proves those optional BiDi modules are unnecessary for the installed Chrome channel.
- Negative packaging finding: an earlier MCPB pack produced 2,209 files / 47.8 MB unpacked / 15.3 MB compressed because `node_modules` was included. The self-contained `dist/server.mjs` runtime means `node_modules/` must stay excluded from MCPB and git.
- Negative privacy finding: the earlier MCP smoke used the real `~/.glovo/session.json` by default and printed exact location plus persistent device/perseus identifiers. Default smoke now uses a temp empty session and the harness prints only booleans/counts; authenticated smoke remains sanitized.
- Basket mutation safety fix: added salted recursive canonical basket snapshots, 0700/0600 private recovery snapshots, bounded retry restore in `finally`, and contract tests for failure after add, after set, transient cleanup get/update/remove failures, deep nested fingerprint sensitivity, option-bearing original line refusal, successful recovery-file deletion, and failed-restore recovery-file preservation.
- Bundled login negative finding reproduced: running the self-contained bundle from an isolated shipped layout with `NODE_PATH` empty failed before Chrome with `Dynamic require of "os" is not supported`. Added the minimal ESM `createRequire`/`__filename`/`__dirname` build banner plus root `package.json` and `browsers.json` runtime data. The isolated shipped-layout bundled login smoke now reaches Chrome and times out without storing credentials.
- Repeat/reorder read-only probe: after two bounded network retries, a live order detail returned `storeId`, `storeAddressId`, and `reorderData.allowed/isAllowed`, but product lines were in `boughtProducts` with names/prices/quantities only and no stable product identifiers. Native reorder mutation endpoint/payload remains unverified, so the release exposes `glovo_preview_reorder` and refuses basket preparation for unsupported lines.
- Product boundary clarified: browser observation may use the user's already-authenticated Chrome tabs only when research/session bootstrap or refresh truly needs it. The explicit `glovo_login` tool may open a dedicated Chrome login window for session establishment. Store search, products, history, stats, reorder preview, and basket operations must go through Glovo API/MCP. Added a static regression test so Playwright/browser automation cannot spread outside auth/login code.
- Primary Glovo-owned evidence for basket contract: Cart SDK/schema `https://glovoapp.com/_next/static/chunks/71703-7b85fe7887b21cf4.js` modules 28831/71703, and store-page caller `https://glovoapp.com/_next/static/chunks/app/%5Blanguage%5D/%5Bcountry%5D/%5Bcity%5D/stores/%5BstoreSlug%5D/page-2b8f23bccedda10f.js`. Installed local extension is not independent evidence because it had the same obsolete `attributes`/fallback implementation.
- Live mutation attempt 1: read discovery found a pizza product with 3 option groups / 2 required groups and original basket had 0 lines, but Glovo rejected the add with `PRODUCT_NOT_AVAILABLE` before any basket line was created. Post-check confirmed 0 baskets / 0 lines. This exposed a privacy bug in raw client errors because the customer id appeared in the path; fixed by redacting authenticated customer paths and raw API error bodies.
- Live mutation attempt 2: improved selector required open store and not-marked-unavailable search result, but product-view preflight failed before snapshot/write because the search result had no `productExternalId`. No basket mutation happened; post-check confirmed no recovery dirs, 0 baskets, 0 lines. Selector now also requires `external_id`. Mutation E2E remains unpassed; do not claim add/set/remove/restore evidence yet.
- Live mutation attempt 3: read-only candidate preflight passed all booleans (open store, current availability, product id, external id, required option choices, repeated product-detail fetch). Add still failed with sanitized `PRODUCT_NOT_AVAILABLE` from Glovo before a basket line existed. Restore wrapper completed; post-check confirmed no recovery dirs, 0 baskets, 0 lines. No checkout/order placement occurred. Mutation E2E remains unpassed and needs deeper basket payload discovery before another live attempt.
- Read-only payload audit found likely root cause: Glovo search/product-detail expose three distinct identifiers (`product_id`, `external_id`, `store_product_id`) and the old E2E/add path only passed product/external ids, causing `basketProduct()` to fall back `storeProductId` to the external id. Added preflight requiring `store_product_id`, fresh detail/search id match, passing fresh `storeProductId` into add, and a contract test that distinct ids survive unchanged into create-basket payload. `storeCategoryId=0` remains a separate hypothesis, not conflated with this fix.
- Second read-only payload audit found product-detail action data also carries a nonzero `storeCategoryId`; current create-basket defaulted it to 0. Added product-view extraction, preflight requirement, local fail-fast when new basket creation lacks nonzero category, MCP fallback from fresh product view, and contract coverage that missing fields fail before POST while distinct ids/nonzero category survive unchanged.
- Session directory negative finding: `~/.glovo/session.json` was `0600`, but the parent `~/.glovo` directory could remain `0755`. Session load/save now hardens the parent directory to `0700`; deterministic regression starts from a deliberately permissive temp directory and proves migration.
- Bundled MCP guest-smoke negative finding: the Playwright-bundled runtime could shadow/break bare `fetch` in the isolated bundle. API transport now calls `globalThis.fetch` explicitly at both direct HTTP sites, with a source-level regression guarding against bare `await fetch(` in `src/glovo/api.mjs`.
- Live exact-contract mutation reached add and set stages with selected customizations visible. Child remove then failed with sanitized `NoResourceFoundException`, likely because the basket product id can rotate after quantity PATCH; post-check showed restore recovered the original `0` baskets / `0` lines and no recovery dirs remained. Stage checks now use bounded retried predicates, remove uses the fresh post-set basket product id, and only `id_rotated` boolean is logged.
- Official restore endpoint evidence from Glovo Cart SDK: `DELETE /v1/authenticated/customers/{customerId}/baskets/{basketId}` deletes an empty or stale selected-store basket. Restore now uses global `getBaskets()` as source of truth, parent-deletes the selected-store basket when the original had none, preserves unrelated baskets, never child-deletes in that branch, treats 404/no-resource as an idempotency hint only, and requires canonical global proof before deleting private recovery snapshots.
- Unit test negative finding: `basket-restore.test` leaked private `glovo-basket-*` snapshot directories. The test now removes the setup and intentional failure snapshots and asserts no net temp-dir leak under `os.tmpdir()`.
- API-only location contract evidence from official Glovo static chunks: public autocomplete `/v3/addresslookup/pub/address`, public resolve `/v3/addresslookup/pub/{placeId}`, and guest serviceability `/customer_profile/api/v1/guest/address_book/delivery_point_info`. Implemented only `glovo_search_locations` and `glovo_select_location`; no saved-address/profile mutation and no browser. Updated web app header version to `v1.2476.1`.
- Independent sanitized live location smoke used a temp session and public landmark, returned 4 compact suggestions with provider, selected a serviceable location with city/country codes, wrote session `0600` under a `0700` temp dir, then deleted the temp state. No real user location/session changed.
- Live mutation attempt 4 after official payload/customization fix: read-only preflight was all true; add stage passed with selected customizations visible; set stage passed with quantity 2 and `id_rotated:false`; child product DELETE still returned sanitized `NoResourceFoundException`. Restore recovered original `0` baskets / `0` lines with no recovery dirs. Official caller audit then found the root cause: current Glovo UI does not use child DELETE or filtered PUT for removal; it calls `PATCH /products/quantity` with `{handlingStrategy,basketVersion,products:[{basketProductId,quantity:0}]}`. `removeFromBasket` and `setQuantity(quantity=0)` now use PATCH-zero.
- Live mutation attempt 5 passed: read-only preflight was all true, private snapshot started with `0` baskets, add stage showed line present and selected customizations true, set stage showed quantity 2 and `id_rotated:false`, remove stage showed line absent in both store and global basket reads, and restore fingerprint matched the original salted fingerprint. Bounded post-check showed `0` baskets / `0` lines and `0` recovery dirs. No checkout/order placement occurred.
- Earlier final local verification passed with `npm run verify`: build, unit/contract tests, strict Claude plugin validation, MCPB validate/pack, guest MCP smoke, isolated bundled-login runtime smoke, dependency audit, and secret scan. MCPB pack contains 6 files, 1.4 MB compressed / 7.9 MB unpacked, with no `node_modules`.
- Bundled login smoke reached Chrome navigation from the isolated packaged runtime with `NODE_PATH` empty; Glovo login page intermittently returned `ERR_CONNECTION_REFUSED`, recorded as network reachability rather than packaging failure.
- Current local `gh auth status` still reports the `denya` keyring token as invalid. SSH over port 443 is the working publish path; this task set local `origin` to `ssh://git@ssh.github.com:443/denya/glovo-skill.git` but did not push.
- Final independent audit found the proven-wrong child-product DELETE helper still present as dead production code even though no MCP tool called it. Removed the helper before release so future callers cannot regress from the official PATCH-zero removal contract; parent-basket DELETE remains restore-only.
- Product-boundary correction: the isolated bundled-login smoke that opened/navigated a separate Chrome window was a negative finding. Automated verification must not launch or drive a browser. `scripts/test-bundled-login.mjs` now copies only the shipped layout into a temp root, starts the bundled MCP server with `NODE_PATH` empty, verifies package/runtime files, confirms `glovo_login` is registered, and calls only `glovo_auth_status` against an empty temp session. Browser use remains limited to existing-tab observation for research/session bootstrap when truly needed, or the explicit `glovo_login` tool's dedicated Chrome login window for customer session establishment; all store/product/history/stats/basket/location/E2E behavior stays API-only.
- No-browser verification rerun passed: `npm run verify` now uses `test:packaged-runtime`, which proves shipped runtime/login-tool structure without opening Chrome or navigating any browser. The browser-boundary regression message now says browser automation is limited to user-initiated login/session bootstrap.
- GitHub's release form rejected the validated `.mcpb` upload with a generic processing error, and the visible authenticated browser moved to unrelated user tabs during the retry. Committing the MCPB to the repo was the wrong workaround; match the Ametller sibling pattern instead: keep `*.mcpb` ignored in Git, deliver local test builds to iCloud Inbox, and publish the approved package as a GitHub release asset.
- First clean Claude plugin install exposed a redundant second GitHub clone from the marketplace entry; that clone chose SSH port 22 and failed in a restricted network. Changed the plugin source to the already-cloned marketplace root (`"./"`), matching the working Ametller marketplace layout, so installation needs only the marketplace clone.
- Final clean published-form install passed from a fresh `CLAUDE_CONFIG_DIR`: marketplace add succeeded, `glovo@denya-glovo` installed at version `0.1.0` from commit `a508966`, and the installed bundled server passed the no-browser packaged-runtime smoke with `glovo_login` registered and an empty temporary session.
- Desktop release `v0.1.1` published via GitHub CLI/API only, preserving the older public `v0.1.0` tag. Release asset `glovo-skill-0.1.1.mcpb` SHA256 is `1dbbe5207606671d58b474d7056b5ed492ff72a6ecd51247b22d2d1b2b4e479d`. Public release download was fetched back, matched the local validated MCPB byte-for-byte, and the downloaded bundle passed the no-browser packaged-runtime smoke. Fresh Claude marketplace add/install from an isolated `CLAUDE_CONFIG_DIR` installed `glovo@denya-glovo` version `0.1.1`.
- Saved-delivery-location gap from live basket task: official web chunk `89139-359e04fe5d8134c3.js` calls authenticated `GET /customer_profile/api/v1/address_book/me/addresses`, filters `entryType === "SAVED_ADDRESS"`, and uses `entry.address`. A sanitized live probe confirmed 5 saved entries and address keys including id/addressLine/details/codes/coordinates without printing address values. Implemented only read-only `glovo_get_saved_locations`; account-level switch/reconfirm remains unimplemented because no safe current web switch endpoint was proven.
- Private saved-location live proof used runtime-only address text from the source task and printed only booleans: `matched=true`, `set_location_args_complete=true`, and `current_location_already_matches=true`. No address text, ids, coordinates, tokens, scripts, fixtures, or location mutation were created.
- Current `gh auth status` succeeds for `denya` through keyring, while Git remote push still uses SSH over port 443. This supersedes the earlier invalid-token negative but preserves that history.
- README/SKILL parity update intentionally copied Ametller's reader journey and routing style only. Did not cargo-cult Ametller's offline Gmail tickets, analytics dashboards, or predictor structure because Glovo's current repo surface is live API locations/stores/history/basket, not receipt ingestion.
- Desktop release `v0.1.2` published via GitHub CLI/API only, preserving `v0.1.0` and `v0.1.1`. Release asset `glovo-skill-0.1.2.mcpb` SHA256 is `f3b4ac5dfab66ceeb121e0d74bebd6084e13887ce1d5cc3f616cca611ae35949`; GitHub reports the same asset digest. Public download matched the frozen local MCPB byte-for-byte, unpacked isolated startup passed with `NODE_PATH` empty and no browser, and a fresh `CLAUDE_CONFIG_DIR` marketplace add/install installed `glovo@denya-glovo` version `0.1.2` with 21 tools including `glovo_get_saved_locations`.
- Current Glovo-owned web evidence: city page chunk `https://glovoapp.com/_next/static/chunks/app/%5Blanguage%5D/%5Bcountry%5D/%5Bcity%5D/page-e38da5c28bcd9ccc.js` calls store content, store fees, restrictions, store info, and similar-store APIs; cart chunk `https://glovoapp.com/_next/static/chunks/71703-7b85fe7887b21cf4.js` remains the basket contract source; route chunk `https://glovoapp.com/_next/static/chunks/20001-2a8082ed3054b309.js` builds an order-summary URL with optional `reorderUrn`.
- Authenticated-vs-guest store-content probe proved the same store returned 20 Top Sellers products to both, while authenticated content added 20 Easy Reorder products with complete current product/external/store-product identifiers. Implemented compact authenticated recommendations; no browser.
- Negative native-repeat finding: current order detail includes `reorderData.allowed/isAllowed`, `isRemake`, store ids, and `boughtProducts` names/quantities/prices/customization text, but no stable product ids. Public chunks expose `reorderUrn` navigation, not a proven direct basket-only reorder endpoint/payload. Implemented read-only current-catalog resolution instead of guessing a native mutation.
- Root bug fixed: `compactOrderDetail` ignored the live `boughtProducts` field, so `glovo_get_order_items` could return zero items even when detail contained them. It now returns items, customization text, pricing lines, status time, store ids, and reorder flags without address/payment payloads.
- Authenticated MCP harness passed with 25 tools. For one live 20-line past order, `glovo_plan_reorder` resolved 16 current products, left 4 unsupported, used 2 bounded fallback searches, and mutated no basket. Easy Reorder returned 10 requested products with complete ids; store pre-check returned one fee range and six public information sections.
- Paced full-history analysis found 946 unique orders across 64 cursor pages and stopped on `no_next_cursor`; 920 cards had parseable visible totals across 218 stores. Bounded enrichment completed 15/15 recent details with no 429, yielding 58 distinct products, 18 with measurable cadence, and 8 with customization history. Item statistics truthfully cover only those 15 detailed orders; no names, order ids, addresses, coordinates, or tokens were logged here.
- Negative harness finding: the old live preflight hardcoded `pizza` and failed when the current saved location exposed only one open food store with no matching pizza. The selector now checks a bounded query list and accepts only a freshly fetched, enabled product with required option groups and all three product ids. Read-only rerun passed with 7 option groups / 3 required groups and every preflight predicate true; no basket write occurred.
